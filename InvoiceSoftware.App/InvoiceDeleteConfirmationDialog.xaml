<Window x:Class="InvoiceSoftware.App.InvoiceDeleteConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delete Invoice" Width="470" Height="235" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="52"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Width="42" Height="42" CornerRadius="21" Background="#FEE2E2" VerticalAlignment="Top">
                <TextBlock Text="!" Foreground="#B91C1C" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Delete this invoice?" FontSize="20" FontWeight="SemiBold" Foreground="#0D2B68"/>
                <TextBlock Margin="0,10,0,0" TextWrapping="Wrap">
                    <Run Text="Are you sure you want to delete invoice "/><Run x:Name="ReferenceRun" FontWeight="Bold"/><Run Text="?"/>
                </TextBlock>
                <TextBlock Text="The invoice will be removed from active lists and any finalized stock quantities will be restored." TextWrapping="Wrap" Foreground="#607089" Margin="0,7,0,0"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancel" Click="Cancel_Click" MinWidth="90" IsCancel="True"/>
            <Button Content="Delete" Click="Delete_Click" MinWidth="90" Background="#B91C1C" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
